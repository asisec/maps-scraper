{
  "name": "maps-scraper-frontend",
  "version": "1.0.0",
  "description": "Maps Scraper AngularJS Frontend",
  "scripts": {
    "start": "http-server . -p 8080 -c-1",
    "build": "mkdir -p dist && cp -r app index.html dist/ 2>/dev/null || xcopy /E /I /Y app dist\\app && copy index.html dist\\",
    "lint": "eslint app/**/*.js"
  },
  "dependencies": {
    "angular": "^1.8.3",
    "angular-route": "^1.8.3",
    "bootstrap": "^5.3.3",
    "@fortawesome/fontawesome-free": "^6.5.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
