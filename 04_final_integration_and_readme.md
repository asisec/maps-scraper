# Phase 4: Final Integration, Environment Variables, and Polish

## SYSTEM RULES REMINDER
- Strict adherence to NO comments (except `TODO:`), Turkish UI, English Code, and COMPLETE outputs.

### Tasks for this Prompt:
1. **Environment Setup:** Provide the complete `.env.example` file for Laravel and the configuration constants for AngularJS, ensuring all database, scraper, and Google Maps visual API keys are properly parameterized.
2. **Docker Orchestration Check:** Provide a final `Makefile` or a setup script (e.g., `setup.sh`) that a developer can run to build the Docker containers, install composer dependencies, install npm dependencies, and start the application with a single command.
3. **Documentation:** Create a comprehensive `README.md` (in Turkish) for the GitHub repository explaining how to start the project via Docker, how to access Swagger UI, and how to use the map interface.
4. Ensure all final code logic bridges smoothly between the AngularJS frontend and Laravel backend.

Provide the files and the final Git commands to checkout a branch `feature/final-integration`, commit, merge all branches locally if necessary, and push everything to `asisec/maps-scraper`.